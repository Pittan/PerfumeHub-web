import { DateParsePipe } from './date-parse.pipe';

describe('DateParsePipe', () => {
  it('create an instance', () => {
    const pipe = new DateParsePipe();
    expect(pipe).toBeTruthy();
  });
});
